import { println } from "./modules/println.js";

// One main file for each route (groups, channels...) with modules imported
document.addEventListener("DOMContentLoaded", function () {
    println();
});